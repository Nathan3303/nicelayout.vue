import { withInstall } from "@nice-layout/utils/utils";

import Title from "./src/title.vue";
import EditableTitle from "./src/editable-title.vue";

export const NlTitle = withInstall(Title);
export const NlEditableTitle = withInstall(EditableTitle);
